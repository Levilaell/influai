{
  "name": "influa-pipeline-prototype",
  "version": "0.1.0",
  "description": "Protótipo do pipeline de vídeo para influenciadores de IA (persona -> roteiro -> keyframes -> vídeo -> voz -> montagem)",
  "type": "module",
  "main": "pipeline.js",
  "scripts": {
    "start": "node pipeline.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@fal-ai/client": "^1.2.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}
